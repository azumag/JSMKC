// Create mock functions for token-utils module
export const generateTournamentToken = jest.fn();

export const isValidTokenFormat = jest.fn();

export const isTokenValid = jest.fn();

export const getTokenExpiry = jest.fn();

export const extendTokenExpiry = jest.fn();

export const getTokenTimeRemaining = jest.fn();
