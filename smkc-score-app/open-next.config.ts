/**
 * OpenNext Configuration for Cloudflare Workers
 *
 * Configures the @opennextjs/cloudflare adapter to build the Next.js app
 * for deployment on Cloudflare Workers. OpenNext converts the Next.js
 * build output into a format compatible with the Workers runtime.
 */

import { defineCloudflareConfig } from '@opennextjs/cloudflare';

export default defineCloudflareConfig({});
