# TAバトルロワイヤル運用仕様

## 1. 競技形式の選択

トーナメント作成時に、TAの競技形式を次の2種類から選択する。

- **通常TA**: Phase 1 → Phase 2 → Phase 3。Phase 3は3ライフで、残り8人・4人・2人の時点でライフを回復する。
- **TAバトルロワイヤル**: TA予選へ登録された全参加者がPhase 1/2を経由せずPhase 3へ進む。初期10ライフ、ライフ回復なし、個別ハンデあり。

バトルロワイヤルを選択した場合は、作成前の確認ダイアログでルールと変更ロックを確認する。TA参加者、ラウンド、サドンデスのいずれかが作成された後は、APIを直接呼んでも競技形式を変更できない。

## 2. 開始条件と進行

バトルロワイヤルのPhase 3開始には2人以上のTA予選参加者が必要である。開始時に全参加者をPhase 3へコピーし、各参加者へ10ライフと大会内ハンデを引き継ぐ。

各ラウンドでは補正後タイムの昇順で順位を決め、参加者数の下位半分が1ライフを失う。奇数人数の場合、上位側が1人多くなる。

- 通常TAの8人・4人・2人ライフ回復は適用しない。
- 通常TAの失格人数上限やrevival判定は適用しない。
- 同じラウンドで複数人が0ライフになった場合、対象者を全員失格にする。
- 0ライフかつ未失格の状態を残さない。
- 最後の1人が優勝者となる。

順位境界の同タイムや3位決定条件など、既存のサドンデス判定は維持する。

## 3. 大会内ハンデ

使用できるハンデは `0秒`、`-1秒`、`-3秒`、`-5秒` の4種類である。

- `Player.taHandicapSeconds`: 新しい大会へ登録するときの既定値。
- `TTEntry.taHandicapSeconds`: その大会で実際に使用するスナップショット値。
- ラウンド結果の `handicapSeconds`: そのラウンドで実際に適用した監査値。

TA予選登録時に選手既定値を大会内エントリへコピーし、Phase 3昇格時にその大会内値を引き継ぐ。登録後に選手マスタを変更しても、既存大会の計算結果には影響しない。

Phase 3開始前は管理画面から単件・一括変更、または選手既定値への復元ができる。Phase 1/2/3エントリが作成された後は、更新APIが `409 TA_HANDICAP_LOCKED` を返す。

## 4. タイム計算とリトライ

通常入力では次の式で補正する。

```text
補正後タイム = max(0, 生タイム + ハンデ秒 × 1000)
```

例: 生タイム104,000ms、ハンデ-5秒の場合、補正後は99,000msとなる。

リトライは既存の固定ペナルティタイムを使い、ハンデを適用しない。保存結果は次を含む。

- `rawTimeMs`: 入力またはリトライ固定値。
- `handicapSeconds`: 実際の適用値。非適用時は0。
- `timeMs`: 順位決定に使用した補正後値。
- `isRetry`: リトライかどうか。
- `tvNumber`: 使用TV番号。未指定はnull。

結果送信前の確認ダイアログと保存後のラウンド履歴は、同じ共通計算・正規化関数を使用する。

## 5. APIと表示

`GET /api/tournaments/:id/ta/phases` はライブ・アーカイブともに次を返す。

- `taMode`: `standard` または `battle_royale`
- `taBattleRoyaleMode`: 既存クライアント互換用boolean
- `phase3Rules.initialLives`
- `phase3Rules.lifeResetThresholds`
- `phase3Rules.survivorsNeeded`
- `phase3Rules.handicapEnabled`
- `phase3Rules.retryAppliesHandicap`

決勝画面はこのレスポンスだけでルールを表示する。残ライフはハートを10個並べず、`♥ 7/10` の形式で表示する。履歴はバトルロワイヤル時に生タイム、ハンデ、補正後タイムを表示する。

## 6. アーカイブ

新規アーカイブはschema version 2で保存し、競技形式、確定ルール、大会内ハンデ、生タイム・ハンデ・補正後タイムを含む。

schema version 1は通常TAとして正規化する。

- 初期3ライフ
- 回復閾値8/4/2
- ハンデ無効
- 欠損した生タイムは`timeMs`
- 欠損したハンデは0

アーカイブ再生では、保存済みエントリ状態とラウンドの`eliminatedIds`・`livesReset`を優先し、現在の選手マスタや大会設定を参照しない。

## 7. 検証コマンド

```bash
npm test -- --runInBand
npm run lint
npm run format:check
npm run build
node e2e/tc-ta-battle-royale.js
```

プレビュー環境では次を使用する。

```bash
npm run e2e:preview:ta-br
```

## 8. 手動受け入れ確認

1. 通常TAとバトルロワイヤルを作成画面で比較できる。
2. バトルロワイヤル作成前に確認ダイアログが出る。
3. 6人を登録し、大会内ハンデを一括設定できる。
4. Phase 3開始後にモード・ハンデ・参加者変更が拒否される。
5. 10ライフ、回復なし、補正後順位が画面に表示される。
6. 6人中下位3人が同時に0ライフとなった場合、3人全員が失格する。
7. リトライへハンデが適用されない。
8. ライブ削除後もアーカイブで同じルール・残ライフ・結果を再現できる。
9. 通常TAの3ライフ・8/4/2回復・ハンデなしが維持される。
